- type: InternationalizationFilter
  fields:
    type: GetSecretValueFilter
  logging:
    fatal: "Error in the AWS Secrets Manager filter. Error: ${circuit.exception}"
    failure: "Failed in the AWS Secrets Manager filter"
    success: "Success in the AWS Secrets Manager filter"
